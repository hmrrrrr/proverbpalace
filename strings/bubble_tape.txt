:turn_tile_to_status:
	turn the tile into this status if it doesn't have it already.

:tile_is_status:
	if the tile has this status, then

:get_spelled_status:
	if this word is the name of a status, then

:get_spelled_word:
	if you currently spell a word, then

:append_cow_bigram:
	if the tile is not shimmering or mystery, and is a single letter, turn it into its respective 100 Cow Lead bigram.

:has_fib_hp:
	if your character's HP is in the Fibonacci sequence, then

:make_capital_wildcard:
	make the tile into a capital wildcard.

:tile_is_single_number:
	if the tile's face is a single number, then

:tile_is_at_least_two_value:
	if the tile has a value of 2 or higher, then

:face_all_from_back_half_of_alphabet:
	if the tile has a single face, and each letter in the face is from the second half of the alphabet, then

:get_random_defensive_status:
	take the status "{status}," then

:pick_random_tile:
	pick a random tile.

:tile_is_faceless:
	if the tile has no face, then

:word_is_in_category:
	if this word is in the category "{category}", then

:get_country_code:
	take your country code as an n-gram, then

:get_country_code:
	take a random suffix as an n-gram, then

:place_ngram_slashed:
	turn the face of the tile into a slashed version of this n-gram.

:place_ngram:
	turn the face of the tile into this n-gram.
